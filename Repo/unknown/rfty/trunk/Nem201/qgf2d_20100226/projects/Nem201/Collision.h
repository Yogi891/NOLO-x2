#ifndef _COLLISION_H_
#define _COLLISION_H_

#include <iostream>
#include <vector>
#include "BMLScript.h"

class Player;
class BasicBullet;
class Enemy;
class BEnemy;
class Test_Bullet;

class Collision
{
public:
	/////////////////////////////////////////////////////////////////////////////////
	//D'tor
	/////////////////////////////////////////////////////////////////////////////////
	~Collision();

	///////////////////////////////////////////////////////////////////////////////////
	//Returns the singleton instance
	///////////////////////////////////////////////////////////////////////////////////
	static Collision& GetInstance();

	//////////////////////////////////////////////////////////////////////////////////
	//Updates all the collison for this frame
	// It requires the player and all current bullets on screen
	//////////////////////////////////////////////////////////////////////////////////
	void Update(Player& a_Player, std::vector<BasicBullet*> a_PlayerBullets, std::vector<Enemy*> a_Enemies);
	void Update(Player& a_Player, std::vector<BasicBullet*> a_PlayerBullets, std::vector<BEnemy*> a_BEnemies, std::vector<Test_Bullet*> a_enBul); // Just some quick hax for now

private:
	/////////////////////////////////////////////////////////////////////////////////
	//Singleton c'tor
	/////////////////////////////////////////////////////////////////////////////////
	Collision();
	
	//
	//singleton instance
	//
	static Collision* m_pInstance;

};


#endif